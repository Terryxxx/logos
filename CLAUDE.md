# CLAUDE.md

Claude Code reads this file. The actual rules live in
[`AGENTS.md`](./AGENTS.md) so they stay in sync across every CLI
that follows either convention (Copilot CLI, Codex, OpenCode all
read `AGENTS.md`).

**Please read `AGENTS.md` in this directory before doing anything.**
