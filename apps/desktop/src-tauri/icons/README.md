# Tauri icons

Tauri requires platform icon assets. Generate them once via:

```bash
cd apps/desktop/src-tauri
pnpm tauri icon path/to/source-512.png
```

For V0.1 development without icons, `pnpm tauri dev` still runs (a warning is printed).
